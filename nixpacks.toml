[phases.setup]
nixPkgs = ["chromium", "chromedriver"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn tracker:app --workers 1 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT"
